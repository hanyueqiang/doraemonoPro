import React, { Component } from 'react';
import { Card } from 'antd';

export default class Content extends Component {
  render() {
    return (
      <div style={{ marginTop: 10, height: '100%', background: '#fff', padding: 15 }}>
        canvas
      </div>
    )
  }
}
